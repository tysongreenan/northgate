/**
 * Empty slot. Enable in lib/hosts.js and add the match to manifest.json.
 * Implement composer bind here — do not put Gemini selectors in core.
 */
export const id = "gemini";
export function init() {}
